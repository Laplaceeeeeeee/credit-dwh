# 简历项目材料 · 金融信贷离线数仓

> **用途**：把阶段二成果写入研究生实习简历
> **数据基准**：全量 2,260,668 行，2026-09-17 实测，质量校验 11/11 通过
> **投递方向**：银行/券商 数据开发（数仓方向）
> **配套**：`docs/信贷数仓实施手册.md`（技术细节）、`docs/名词讲解.md`（术语）

---

## 一、把这段写进简历的哪个位置

| 位置 | 写什么 | 篇幅 |
|---|---|---|
| **项目经历** | 主战场。用 §二 的版本 A | 4–5 条 |
| **专业技能** | 从 §四 里挑你真会的 | 1–2 行 |
| **自我评价** | 别写。用项目经历说话更硬 | 不写 |
| **GitHub** | 放链接，让面试官自己看代码 | 1 行 |

> ⚠️ **只有这一个项目时，把它放在"项目经历"第一条**，不要塞进"其他"。

---

## 二、项目经历正文（三个版本，按需选）

### 版本 A · 标准版（推荐，占 5 行）

```
金融信贷离线数仓（个人项目）｜2026.11–2027.01｜GitHub: github.com/Laplaceeeeeeee/credit-dwh
· 基于 LendingClub 公开信贷数据（2,260,668 笔贷款 / 137 字段），设计并实现 ODS-DWD-DWS-ADS
  四层数仓，产出 13 张表：申请时点事实表、表现期事实表、月末快照事实表、3 张维表、4 张 ADS 指标表
· 主动划分"申请时点信息/表现期信息"并做物理分表，配合字段黑名单自动校验阻断数据泄漏；
  识别数据仅含放款样本的幸存者偏差与 member_id 整列缺失，删除批准率指标、将客户维降级为
  申请人画像维，并在文档中声明影响边界
· 实现 8 项数据质量校验的可执行框架：行数守恒、主键唯一、汇总一致性（ADS 汇总与 DWD 明细
  重算完全一致）、枚举合法、分区完整性、无未来信息，全量跑批 11/11 通过
· 基于 4,471 万行月末快照构建 Vintage 逾期率矩阵与迁徙率矩阵（账龄对齐后比较批次风险），
  定位 DPD_31_120→DPD_120_PLUS 迁徙率达 50.87%，为风险加速恶化关键节点
· 定位并解决快照表写入性能问题（主键顺序与插入顺序不匹配致聚簇索引随机写），调整主键为
  与写入同序并改为"删索引-灌数-重建索引"流程，写入吞吐 5,379→38,032 行/秒，480 万行
  灌数耗时从 26 分钟降至 4.2 分钟
```

### 版本 B · 精简版（只有 3 行空间时）

```
金融信贷离线数仓（个人项目）｜2026.11–2027.01
· 基于 226 万笔公开信贷数据设计并实现 ODS-DWD-DWS-ADS 四层数仓（13 张表），
  以申请时点/表现期物理分表 + 字段黑名单自动校验阻断数据泄漏
· 建立 8 项数据质量校验框架（含汇总一致性、无未来信息），全量跑批 11/11 通过；
  识别仅含放款样本的幸存者偏差与 member_id 整列缺失，删除批准率并声明数据边界
· 基于 4,471 万行月末快照产出 Vintage 与迁徙率矩阵；通过主键顺序优化将快照写入吞吐
  提升 7 倍（5,379→38,032 行/秒），480 万行灌数从 26 分钟降至 4.2 分钟
```

### 版本 C · 详细版（网申长表格、作品集文档用）

在版本 A 基础上追加：

```
· 技术栈：Python(pandas) · SQL(MySQL 8) · Docker/Linux · Git；涉及维度建模、增量加工、
  数据质量治理、性能调优
· 交付物：13 张表 DDL、12 个 ETL 模块、1 份指标口径字典（8 项指标）、1 份数据边界说明
  （10 项）、数据探查报告、数仓设计文档（含架构图与星型模型图）、分析报告与 4 张图表
· 工程化：一键流水线（8 步顺序编排、任一步失败即中止）、增量水位表、运维手册
```

---

## 三、支撑材料：每个数字的来源

> 🔑 **面试官问"这个数字怎么来的"，你要能当场答出**。下表是备用弹药。

### 3.1 数据事实（全部本地实测）

| 项 | 值 | 怎么测的 |
|---|---|---|
| 原始文件 | 374.4 MB（`accepted_2007_to_2018Q4.csv.gz`） | 文件属性 |
| 物理行数 | 2,260,702 | `gzip` 逐行扫描 |
| **页脚行（剔除）** | **33** | 全文件扫描识别非数字主键 |
| **真实数据行** | **2,260,668** | 2,260,702 − 1 表头 − 33 页脚 |
| 字段数 | 151 → **保留 137** | 剔除 14 个整列为空 |
| 放款时间跨度 | **Apr-2008 ~ Sep-2018** | `MIN/MAX(issue_d)` |
| 主键 `id` | 完全唯一，存为 string | `nunique()` 校验 |

### 3.2 业务指标（全量）

| 指标 | 值 |
|---|---|
| **终态不良率** | **19.9807%**（269,360 / 1,348,099） |
| ❌ 错误口径（不限定终态） | 11.9151% —— **低估 8.07 个百分点** |
| 快照表行数 | 44,718,091（账龄 MOB ≤ 24） |
| Vintage 矩阵 | 24,768 行 |
| 迁徙率矩阵 | 108 行（12 个观测月） |

### 3.3 风险分层（单调性完美，约 8.2 倍跨度）

| 评级 | 笔数 | 不良率 | 加权平均利率 |
|---|---|---|---|
| A | 235,193 | **6.04%** | 7.11% |
| B | 393,102 | 13.40% | 10.66% |
| C | 382,323 | 22.44% | 14.07% |
| D | 201,657 | 30.38% | 17.76% |
| E | 94,192 | 38.43% | 21.14% |
| F | 32,306 | 45.15% | 24.92% |
| G | 9,326 | **49.67%** | 27.61% |

**FICO 分层**：660-699 → 23.60%；700-739 → 16.08%；740-779 → 10.57%；≥780 → **7.11%**

### 3.4 Vintage（2016-01 批次为例）

| 评级 | MOB12 | MOB24 |
|---|---|---|
| A | 0.48% | 2.77% |
| C | 2.30% | 11.53% |
| E | 5.95% | 22.49% |
| G | 12.89% | **36.60%** |

**各批次 MOB24**：2014-01 批次 7.55% → 2016-01 批次 **10.41%**

### 3.5 性能优化（最有分量的技术点）

| 项 | 优化前 | 优化后 |
|---|---|---|
| 快照表主键 | `(loan_id, snapshot_month)` | **`(snapshot_month, loan_id)`** |
| 写入吞吐 | 5,379 行/秒（**持续衰减**） | **38,032 行/秒（恒定）** |
| 480 万行灌数 | 26 分钟 | **4.2 分钟** |
| 全量 4,471 万行 | 无法完成 | 22.4 分钟 |

**根因**：数据按观测月批量写入，而主键以 `loan_id` 打头，
导致同一笔贷款的各月记录在聚簇索引中分散 → 随机 I/O + 页分裂。

---

## 四、技能清单（挑真会的写，别全抄）

### 4.1 可以放心写（本项目亲自动手且能讲清）

```
· SQL：窗口函数、多表关联、自关联、子查询；分区/索引设计与查询调优
· 数仓建模：分层架构（ODS/DWD/DWS/ADS）、维度建模（星型模型、事实表与维表、周期快照事实表）
· 指标与口径治理：指标口径字典、口径代码化、数据边界声明
· 数据质量：行数守恒、主键唯一、汇总一致性、枚举合法、**数据泄漏防控**
· Python 数据处理：pandas 大数据集分块读写、Parquet 列存、类型标准化与清洗
· 数据库：MySQL 8（DDL、字符集与排序规则、批量写入、索引与主键顺序对性能的影响）
· 工程实践：Docker、Linux 基础、Git、一键流水线编排、运维手册编写
```

### 4.2 ⚠️ 不要写（本项目没做，写了会被追穿）

| 不要写 | 原因 |
|---|---|
| Hive / Spark / Hadoop | **阶段三才做**，现在完全没碰 |
| 调度平台（DolphinScheduler / Airflow） | 本阶段用脚本替代，没配过 DAG |
| Flink / Kafka / 实时数仓 | 阶段三内容 |
| 机器学习 / 建模 / AUC | 明确没做，那是算法岗 |
| 数据湖（Iceberg / Hudi） | 没做 |
| "精通"任何东西 | 学生写"精通"是负分 |

### 4.3 面试问到时怎么答（关于阶段三技术）

> "Hive/Spark 我在下一阶段会做 —— 用同一套口径迁到 Hive 分区表做对照实验，
> 量化单机与分布式的性能差异，明确这个数据量的引擎选择边界。
> 目前的判断是：**226 万行、全量约 6.8 GB，单机 MySQL 完全能承载**，
> 所以我没有为了用大数据而用大数据。"

---

## 五、⭐ 面试问答准备

### 5.1 层次 1：做了什么（3 分钟，照着版本 A 讲）

**目标**：让面试官 30 秒内明白你在做什么、有什么价值。

> "这是一个**金融信贷领域的离线数仓项目**。
> 我把一份 226 万行的原始信贷放款流水，加工成四层数仓 ——
> ODS 原样落地、DWD 清洗成明细、DWS 按月做快照、ADS 出指标表，一共 13 张表。
> 目的是回答三个风控问题：这笔贷款会不会逾期、哪批客户质量在恶化、风险往哪个方向走。
> 项目是**数据工程**，不是机器学习建模。"

### 5.2 层次 2：为什么这么做（4 分钟）

**必答的三个"为什么"**：

| 问题 | 回答要点 |
|---|---|
| **为什么要分四层** | 为了**解耦**。业务逻辑变了只改 DWD，口径变了只改 ADS，ODS 永远不动。每层可独立验证，出错能快速定位 |
| **为什么申请时点和表现期要分两张表** | 信贷数据的经典陷阱：`loan_status`、`out_prncp` 这些是**放款后才知道**的。混用会造成**数据泄漏**，指标看着漂亮但完全错 |
| **为什么用月末快照而不是拉链表** | 本数据是**放款时点的静态快照**，没有真实的属性变更序列。硬做 SCD2 只能造假数据。快照表是**标准数仓模式**且数据真实可校验，还天然支撑 Vintage 分析 |

### 5.3 层次 3：遇到什么问题、怎么解决 ⭐（5 分钟，分水岭）

**准备三个有细节的故事**：

#### 故事一：33 行散布的页脚（数据质量）

> "这个文件不是一份单纯导出，而是**多个年度分段拼接**的，
> 段间夹着章节标题和汇总行。总共 **33 行**非数据行，**散布在文件中部**，
> 不是只在末尾。
>
> 我一开始用 `skipfooter=1` 只跳最后一行，结果另外 32 行混进了 `id` 列，
> 还让 pandas 把主键推断成字符串，引发后续 schema 不匹配。
> 后来改成**全文件扫描**，按'`id` 必须是纯数字'判定剔除。
>
> 这也解释了一个早期误判：我原以为'有 32 行缺 `issue_d`'，
> 实测证明**那就是这些页脚行**，不是真实记录缺字段。"

#### 故事二：快照表写入性能（最有分量）

> "快照表最初写入很慢，480 万行要 **26 分钟**，而且**吞吐持续衰减** ——
> 从 40,000 行/秒一路掉到 5,000 行/秒。
>
> 我分析下来根因是**主键顺序与插入顺序不匹配**：
> 主键是 `(loan_id, snapshot_month)`，但数据是**按观测月批量写入**的。
> 这样同一笔贷款的不同月份记录在 InnoDB 聚簇索引里分散在很远的位置，
> 造成随机 I/O 和页分裂。
>
> 我把主键改成 `(snapshot_month, loan_id)`，让逐月插入变成聚簇索引的**顺序追加**；
> 同时改成'灌数前删二级索引、灌完再重建'。
> 结果 480 万行从 26 分钟降到 **4.2 分钟**，吞吐稳定在 38,000 行/秒不再衰减。"

#### 故事三：口径错误（体现业务理解）

> "不良率最初我按'不良笔数 / 总笔数'算，得到 11.92%。
> 后来意识到分母有问题 —— 数据里有 **38.85% 是 `Current` 状态**，
> 也就是**仍在正常还款、结果还没定**的贷款。
> 把它们放进分母，等于把'还没到期的贷款'当成'没出问题的贷款'。
>
> 限定终态后正确值是 **19.98%**，**低估了 8 个百分点**。
> 这是信贷指标里最经典的口径错误。"

### 5.4 层次 4：我的判断与边界 ⭐⭐（4 分钟，真正的分水岭）

> "这份数据有**三个硬限制**，我在设计阶段就处理掉了。
>
> **第一，只有已放款的样本、没有拒绝客群** —— 存在**幸存者偏差**。
> 所以我**删掉了批准率**，因为分母不存在；所有风险结论都限定在'已放款客群'内表述。
>
> **第二，会员 ID 列是空的**（2,260,668 行非空值为 0）。
> 我**没有**用其他字段拼一个伪客户 ID —— 那会造成错误关联。
> 我把客户维降级为**申请人画像维**，并在文档里写明'客户级分析不可做'。
>
> **第三，只有最终状态、没有逐月还款记录**。
> 所以迁徙率的 DPD 档位是我**按规则推导**的，我在文档里标注了假设，
> 也说明了'MOB 小于 12 时只看核销会低估风险'。
>
> 另外，某批次逾期率上升，我能证明的是'该批次 C–E 级占比从 X% 升到 Y%'，
> 但**我不能证明是准入策略放松导致的** —— 那需要申请侧和策略变更数据。
> 我更愿意说清我证明不了什么，而不是给一个漂亮的因果。"

> 🔑 **最后一段极其加分**。绝大多数候选人只会讲"我做了什么"，
> 你能主动说出"我证明不了什么"，面试官会直接给你提一档。

### 5.5 高频追问速答

| 追问 | 回答要点 |
|---|---|
| **不良率怎么算？** | 不良笔数 / **终态**笔数。必须限定 `is_terminal=1`，否则被 `Current` 稀释，低估 8 个百分点 |
| **什么算"不良"？** | Charged Off + Default + 政策外核销，共 269,360 笔。口径写在 `config.py` 里，全局唯一 |
| **加权平均利率为什么加权？** | 简单平均会让 1 千元与 4 万元贷款同权。按金额加权才反映真实资金成本 |
| **Vintage 为什么不能直接比总逾期率？** | 老批次**账龄长、暴露更充分**，直接比会误判。必须**对齐账龄**（MOB）才公平 |
| **汇总一致性怎么校验？** | ADS 按评级汇总的（终态笔数, 不良笔数）= DWD 明细重算值，实测 **1,348,099 / 269,360 完全一致** |
| **为什么这个数据量不需要分布式？** | 226 万行、全量约 6.8 GB，单机可承载。我用分层是为了口径复用与可追溯，**和体量无关**。阶段三会做引擎对照实验量化边界 |
| **你项目里最难的是什么？** | 讲快照表性能优化（故事二） |
| **数据泄漏你怎么防？** | 三道防线：① DDL 物理分表 ② 代码白名单 ③ **字段黑名单自动校验**。不靠自觉，靠机制 |
| **遇到过数据脏在哪？** | 33 行散布页脚、`member_id` 整列空、14 列全空、`dti` 有 999 哨兵值、`annual_inc` 最大值 900 万 |

### 5.6 主动认输题（被问到不会的怎么办）

| 被问 | 诚实且得体的回答 |
|---|---|
| "Hive 建表你会吗？" | "阶段三会做。目前我理解是 Hive 表不强制主键、分区列不放在字段列表里、ORC 列存下分区裁剪收益更明显 —— 但**还没实操过**，不敢说会" |
| "数据量再大十倍怎么办？" | "字节数到几十 GB 我会迁到 Hive/Spark，用分区裁剪和列存减少扫描；再大考虑分区分桶。**不过我没在真实集群上验证过**" |
| "你做过实时吗？" | "没有。阶段三计划用 CDC + Kafka 做一条实时链路，但目前**只做过离线**" |

> 🔑 **说"我没做过"不可怕，把没做过的说得像做过才可怕。**
> 面试官最反感的是编，最喜欢的是**清楚自己边界的人**。

---

## 六、写作规则与禁忌

### 6.1 四条硬规则

1. **每个数字必须实测** —— 上面所有数字我都跑过，你也要能当场复现
2. **不写做不到的** —— 面试会验证你写的每一个词
3. **必须放 GitHub 链接** —— 从"我说我会"到"你自己看"
4. **一条 bullet 一件事** —— 不要塞两件事然后都讲不清

### 6.2 五个禁忌

| ❌ 不要写 | 为什么 |
|---|---|
| "精通 SQL / Python" | 学生写"精通"是负分。写"能独立完成 XXX" |
| "负责数据仓库建设" | 个人项目不叫"负责"，写"设计并实现" |
| "提升了数据处理效率" | 没有数字 = 没有说服力。写"26 分钟降至 4.2 分钟" |
| "使用了 Hadoop 生态" | 没做过就别写。**这是最容易翻车的一句** |
| "通过数据分析发现了业务问题" | 太虚。写具体发现（如"某批次 MOB24 不良率高出同期 2.9 个百分点"） |

### 6.3 动词表（用这些开头）

**推荐**：设计、实现、构建、定位、识别、优化、校验、建立
**避免**：负责、参与、协助、学习（这些显得你没主导）

---

## 七、投递前自检

- [ ] 所有数字都是**实测值**，且我能当场复现
- [ ] **没写** Hive/Spark/调度/实时（阶段三内容）
- [ ] **没写** 机器学习/建模/AUC
- [ ] 没有"精通""负责"这类虚词
- [ ] **GitHub 链接可访问**，且仓库里没有误传的大数据文件
- [ ] README 能让面试官 1 分钟看懂项目
- [ ] 我能讲出至少 **2 个有细节的技术故事**（页脚、性能优化、口径错误）
- [ ] 我能说出**至少 3 个"我证明不了什么"**
- [ ] 版本 A 的 5 条 bullet，**每条都能展开讲 1 分钟**

---

## 八、一句话总结（面试开场用）

> **"这是一个把 226 万行原始信贷流水，加工成四层数仓的工程项目。
> 我做的核心工作是：把口径定清楚、把数据质量管起来、
> 并且能说清楚这份数据到底能证明什么、不能证明什么。"**

> 🔑 最后半句是区分"学生项目"和"工程思维"的关键。
