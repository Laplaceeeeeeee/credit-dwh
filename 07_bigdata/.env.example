# 阶段三大数据层：镜像 tag 参数化
#
# 用法：复制一份为 .env（.env 已被 .gitignore 忽略，不会入库）
#   Copy-Item 07_bigdata\.env.example 07_bigdata\.env
#
# ⚠️ tag 必须是你实际预检/拉取成功的那个（docker manifest inspect 或 docker pull）
#    本机实测可用的组合（Docker Desktop 已配国内镜像源）：
SPARK_IMAGE=apache/spark:3.5.9-scala2.12-java11-python3-ubuntu
HIVE_IMAGE=apache/hive:3.1.3
METASTORE_DB_IMAGE=mysql:8.4
#
# 备选（按顺序尝试）：
#   apache/spark:3.5.8-scala2.12-java11-python3-ubuntu
#   apache/spark:3.5.3-scala2.12-java17-python3-ubuntu
#   bitnamilegacy/spark:3.5      （冻结镜像，仅过渡）
#
# ⚠️ 不要用 apache/hive:4.0.x —— Spark 3.5 不支持 Hive 4 metastore，
#    会报 thrift "Invalid method name: 'get_table'"。
