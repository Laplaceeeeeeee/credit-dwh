# 阶段三进度与收尾状态（暂时性收尾）

> 更新：2026-09-22 ｜ 对应 `docs/项目指导书v3(阶段三).md`

## 进度：7/9 步完成

| 步 | 内容 | 状态 | 验收记录 |
|---|---|---|---|
| 12 | 大数据环境（Spark + Hive Metastore） | ✅ | `07_bigdata/环境验收记录.md` |
| 13 | 数据迁移 + 双引擎一致性 | ✅ | `07_bigdata/第13步-迁移验收记录.md` |
| 14 | Hive 复现 4 张 ADS + 逐行比对 | ✅ | `08_benchmark/双引擎口径差异.md` |
| 15 | 测量框架 + E1/E2/E3 | ✅ | `08_benchmark/E1_分区裁剪.md`、`E2_小文件合并.md`、`E3_列存格式对比.md` |
| 16 | ⭐ E4 数据倾斜 | ✅ | `08_benchmark/E4_数据倾斜报告.md` |
| 17 | PySpark 重写 + E5 三引擎基准 | ✅ | `08_benchmark/引擎选型结论.md` |
| 18 | ⭐ 工程化收口（单测/CI/血缘/SLA） | ✅ | `docs/第18步-工程化收口验收记录.md` |
| 19 | 实时链路 + 批流对账（可砍） | ⬜ 未开始 | — |
| 20 | 面试资产与收尾 | ⬜ 未开始 | — |

## 已交付的关键数字（面试可直接引用）

- 双引擎一致性：**7/7 指标一致，最大差异 0.0**；4 张 ADS 表**逐行一致**（比率列 5e-5 差异已查明为 MySQL `div_precision_increment=4` 的量化语义）
- E1 分区裁剪：扫描文件 **139 → 1**、扫描字节 **48.54 MB → 0.88 MB**（同谓词非分区对照仍读 47.87 MB）
- E2 小文件：文件数 **2400 → 12**，查询 **2.22 s → 0.28 s（7.9×）**，任务数 75 → 4
- E3 列存：体积 151.68 / 28.85 / 34.37 MB（Text/Parquet/ORC）；少列查询快 3.0×，全列收窄到 2.4×
- E4 倾斜：Fully Paid 占 **47.63%**、倾斜比 **4.29**；max/p50 **4.48 → 1.8**；加盐 10 桶耗时 **7.25 → 2.39 s**；`REBALANCE` 提示 **1.75 s** 最快
- E5 选型：全量 226 万行 MySQL 端到端 **3.94 s** vs Spark **5.07 s**（含 3.94 s 冷启动）；**推荐 MySQL 单机**，外推交叉点约 270 万行
- 工程化（第 18 步）：口径回归单测 **34 条全绿**（不依赖数据库）｜ruff **0 error**（修掉 9 处）｜
  数据血缘 **23 个节点**通过一致性校验（悬空引用 / 双边一致）｜CI 三段式：ruff + pytest + 血缘校验

## 环境现状

- 5 个容器全部 Up（`bd-metastore-db`/`bd-metastore`/`bd-spark`/`bd-spark-worker`/`credit-dwh-mysql`）
- Hive 仓库 794 MB；交换 TSV **3.84 GB 仍在** `07_bigdata/exchange/`（已 gitignore，可删）
- Docker VM 内存 7.64 GB：**跑重任务前先 `docker stop credit-dwh-mysql` 腾内存**
- 开发依赖已装进 `.venv`（pytest 9.1.1 / ruff 0.16.8 / PyYAML 6.0.3，见 `requirements-dev.txt`）
- ⚠️ **阶段三第 12–18 步的提交全部未推送** —— 本地记录的 `origin/main` 停在阶段二的 `89c28d3`，
  也就是**阶段三的代码在 GitHub 上一个都没有**（`git rev-list --count origin/main..HEAD` 可查具体条数）。
  push 失败是本机代理把 `github.com` 分流成直连（TLS 被重置），而 `api.github.com`/`pypi.org` 走代理都通 ——
  **是规则问题不是节点全坏**，见 v3 附录 B-49

## 恢复工作时的入口

```powershell
docker start credit-dwh-mysql
cd 07_bigdata; docker compose -f docker-compose-bigdata.yml up -d
docker ps --format 'table {{.Names}}\t{{.Status}}'
```

下一步从 **第 20 步 面试资产与收尾** 开始（第 19 步实时链路可砍）。
第 20 步要用到的阶段三数字已全部就绪；工程化那套（单测/CI/血缘/SLA）也已可写进简历。
