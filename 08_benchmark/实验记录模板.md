# 实验记录模板（复制成 E1_xxx.md 使用）

> **规则**：每个实验必须留下 **对照组 / 被测量 / 结论**。三者缺一，就不算实验。
> 术语按学习手册 `名词讲解.md` 统一（不入库）；数字必须**可复现**（附一条能重跑的命令）。

---

# 实验 E<N>：<实验名>

> **执行**：`08_benchmark/exp/E<N>_xxx.sql`（harness：`run_experiment.py`）
> **结果**：`08_benchmark/_results/E<N>_xxx.csv`、`file_stats.csv`、`spark_task_stats.csv`
> **日期**：2026-__-__ ｜ 数据：<表名 / 行数 / 体积>
> **环境**：Spark 3.5.9 Standalone（worker 4 核 / 2 GB，executor 1500m）

## 1. 实验目的

<要回答什么问题，一句话>

## 2. 变量设计

| 组别 | 配置 | 说明 |
|---|---|---|
| **Baseline** | <配置 A> | |
| **Treatment** | <配置 B> | |
| **Control** | <同谓词/同数据的对照> | ⭐ 有对照组才能证明"是 X 起作用，不是别的" |

**控制变量**：数据、查询、集群资源、并行度必须完全相同；重复次数与取值方式（中位数）要写明。

## 3. 被测量

| 指标 | 怎么测 | 数据来源 |
|---|---|---|
| 扫描文件数 / 字节数 | Spark SQL 指标 `number of files read` / `size of files read` | 事件日志 → `sql_scan_metrics.csv` |
| 文件系统事实 | `measure_files.sh`（直接数文件、数字节） | `file_stats.csv` |
| 耗时 | harness 计时（默认 3 次取中位数） | `_results/E<N>_xxx.csv` |
| 任务数 / task 耗时分布 | 事件日志 | `spark_task_stats.csv` |
| 下推/计划证据 | `EXPLAIN EXTENDED` / `EXPLAIN COST` | 保存成 `.txt` |

## 4. 结果

| 组别 | 指标1 | 指标2 | 指标3 |
|---|---|---|---|
| Baseline | | | |
| Treatment | | | |
| **降幅** | | | |

## 5. 结论

1. **量化结论**：从 X 降到 Y，降幅 Z%（用数字说话）
2. **机制解释**：为什么会这样（不是"因为 Spark 更快"这种废话）
3. **适用边界**：什么情况下这个优化不成立 / 会收窄
4. **诚实说明**：哪些数字不可靠（噪声、单次测量、样本不具代表性）

## 6. 复现方式

```powershell
cd F:\BUPT\Internship_Preparation\credit-dwh
New-Item -ItemType Directory -Force 08_benchmark\_spark_events | Out-Null
docker exec -i bd-spark /opt/spark/bin/spark-submit `
  --master spark://spark:7077 --executor-memory 1500m --total-executor-cores 4 `
  --conf spark.eventLog.enabled=true `
  --conf spark.eventLog.dir=file:///workspace/08_benchmark/_spark_events `
  /workspace/08_benchmark/run_experiment.py `
  /workspace/08_benchmark/exp/E<N>_xxx.sql --reps 3
& .\.venv\Scripts\python.exe 08_benchmark\parse_spark_log.py 08_benchmark\_spark_events
docker exec bd-spark bash /workspace/08_benchmark/measure_files.sh <表名>
```

**实测耗时**：__ s

## 7. 踩到的坑（若有）

<新发现的语法/语义/环境问题 → 可复现的部分写进本仓库 `06_ops/runbook.md`；学习手册 `项目指导书v3(阶段三).md` 附录 B 同步一份（不入库）>
