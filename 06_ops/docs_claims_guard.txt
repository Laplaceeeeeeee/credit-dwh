# ============================================================
# 文档过期说法守卫（一行一条；# 开头为注释）
#
# 用途：
#   阶段二时期的 README 里写着"Hive / Spark 阶段三才做""还没实操过"，
#   而阶段三做完后 README 写的是"已完成 Hive/Spark 双引擎比对"。
#   两处并存 = 自相矛盾，读者会先怀疑数字。
#   这份清单把"矛盾"变成机器可查的断言：一旦旧说法回归，验收脚本立即 FAIL。
#
# ⚠️ 为什么放 UTF-8 文本而不是写进 .ps1：
#   BOM-less 的 .ps1 在 Windows PowerShell 5.1 下按 GBK 读，
#   中文会把字符串引号截断 → ParserError（阶段二踩过）。
#   所以 .ps1 本体保持纯 ASCII，中文断言全部走这个边车文件。
#
# 检查范围：README.md（以及本地存在时的其他对外说明文档）
# ============================================================
阶段三才做
阶段三会做
下一阶段会做
现在完全没碰
阶段三计划用
还没实操过
