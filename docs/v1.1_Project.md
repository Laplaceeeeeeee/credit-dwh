# 项目.md v1.1 —— 金融/信贷离线数仓项目 完整实施手册（修订版）

> **v1.0 生成日期**：2026-09-10 ｜ **v1.1 修订日期**：2026-09-12
> 定位：`准备目录.md` **阶段二（2026.11–2027.01中）** 的核心产出物
> 阶段三（2027.02–03）**重新定义**为"引擎迁移与性能基准"，而非"重写一遍"
> 目标：产出一段**能写进简历、能在面试中讲 15 分钟**的信贷数据项目
> 预计投入：**阶段二约 100–112 小时**（其中约 20 小时为 v1.1 新增的工程基线）

---

## 📚 文档体系（先读这里）

本目录下三份文档的关系：

| 文档 | 回答什么问题 | 何时看 |
|---|---|---|
| `项目分析.md` | **v1.0 有什么问题？**（岗位匹配度、深度广度评估、风险点） | 想理解修订理由时 |
| **`v1.1_Project.md`（本文档）** | **做什么、为什么这么做**（规划、设计决策、时间表、话术） | **常读，作为总纲** |
| `项目从零开始指导书.md` | **具体怎么一步步做**（命令、代码、DDL、验收标准、排错） | **动手时逐步骤执行** |

> ⚠️ **本文档 §九 时间表为规划视角**（按周排），
> **指导书 附录 D 打卡表为执行视角**（按步排，合计约 112 小时）。
> 两者数字若有细微出入，**以指导书的实操耗时为准**。
>
> 🔑 **本文档已完整吸收 `项目分析.md` 提出的全部修订建议**（7 项，见下表），无需再对照分析文档执行。

---

## 〇、v1.1 修订说明：改了什么、为什么改

v1.0 的**设计思路是对的**，但存在一个结构性错配和三个会被面试追穿的技术隐患。本次修订逐条处理：

| # | v1.0 的问题 | v1.1 的修订 | 影响章节 |
|---|---|---|---|
| **1** | ⭐ **时序错配**：Hive/Spark/倾斜/调优全部押在阶段三（2027.01–03），**晚于投递季**，导致简历技术栈画像偏"数据分析"而非"数据开发" | 阶段二**用 SQL 完成全部指标实现**（MySQL 分区表 + 增量跑批）；阶段三改为"同一口径的**引擎迁移 + 性能基准**" | §五、§八 |
| **2** | ❌ **"批准率"指标不成立**：数据只有获批样本（`accepted_*.csv`），分母不存在 | **删除批准率**，新增**数据边界与偏差说明**章节，主动写出幸存者偏差 —— 从隐患变成加分点 | §4.1、§4.6 |
| **3** | ❌ **SCD2 拉链表无真实数据支撑**：LendingClub 是单次快照，`annual_inc`/`grade` 都是放款时点值，无法构造真实属性变更 | 用**月末快照事实表（periodic snapshot fact）**替代，同样体现历史追踪能力，且是**真结构** | §3.4 |
| **4** | ⚠️ **业务解读存在过度归因**："可能与客群下沉或授信政策放松有关"在本数据中无法验证 | 改写为**"数据内可证 vs 需外部数据"两段式解读** | §6.2 |
| **5** | ❌ **广度硬缺口**：任务调度、Linux/Shell、Git、SQL 工程化**全文未提**，而这是银行数仓 JD 高频要求 | 新增 **§五 工程基线**（Linux/Docker、Git、调度、增量、质量校验代码化） | §五 |
| **6** | ⚠️ **最强的两个亮点在简历里隐形**：防数据泄漏、数据质量校验体系 §7.2 一字未提 | 简历段落**重写**，两个亮点前置，并加入量化数字模板 | §7.2 |
| **7** | ⚠️ **无任何量化成果**，简历说服力弱 | 新增**量化指标清单**，要求每个阶段记录数字 | §11.2 |

**一句话**：v1.0 的深度够了，问题是**最好的深度排在了投递季之后**，并且有 3 处会在面试中被追穿的隐患。v1.1 把工程能力搬进阶段二，把阶段三变成真正的差异化。

### 〇.2 🚨 本地数据实测修正（2026-09-12 补充，**必读**）

在编写配套指导书时，对你的 `data/accepted_2007_to_2018Q4.csv.gz` 做了实测核验。**发现 5 个会直接影响设计的事实**，已全部落入本文档与指导书：

| # | 实测发现 | 对设计的影响 | v1.1 的处理 |
|---|---|---|---|
| 1 | 文件**真实行数 2,260,700**（含 1 行页脚共 2,260,701），151 列 | ✅ 与公开口径一致，简历可放心写 226 万 | 简历数字**已改为实测值** |
| 2 | 最后一行是**页脚文本** `Total amount funded in policy code 2: ...`，非数据 | 不处理会产生 1 行脏数据，行数守恒校验失败 | 读取必须 `skipfooter=1, engine="python"` |
| 3 | **`member_id` 列整列为空**（2,260,700 行非空值 = 0） | ❌ **"客户维 + 一人多贷"的设计不成立** | **`dim_customer` 改为 `dim_applicant`**（申请人画像维，粒度 = 一笔贷款，主键 = `loan_id`） |
| 4 | **32 行记录 `issue_d` 与 `loan_status` 同时为空** | DWD 行数 ≠ ODS 行数，需登记差异 | DWD 标 `REJECT` 不写入；**行数守恒 = ODS 2,260,700 − 32 = DWD 2,260,668** |
| 5 | `loan_status` 完整分布已知：Fully Paid 1,076,751 / Current 878,317 / Charged Off 268,559 / … | 口径与预期不良率可直接校验 | **不良 = 269,360 笔；终态 = 1,348,099 笔；终态不良率 ≈ 19.98%**（若算出 ~11.9%，说明漏了 `is_terminal=1`） |

> ⚠️ **第 5 条特别重要**：878,317 笔 `Current`（占 38.85%）**仍在还款中**。
> 若不限定终态，不良率会被稀释到约 11.9% —— **这是信贷指标里最经典的口径错误**，也是面试的高频追问点。现在你有了一个可对照的预期值。

> ✅ **好消息**：`id` 列 2,260,700 行**完全唯一、无重复**，主键设计成立。

**关于 `member_id` 的面试话术（新增亮点）**：
> "这份数据的会员 ID 列整列为空，无法识别'同一客户的多笔贷款'。我**没有**用 `annual_inc + addr_state` 之类去拼一个伪客户 ID —— 那会造成错误关联、让客户级统计失真。
> 我把客户维降级为**申请人画像维**，粒度是一笔贷款，并在文档里写明'客户级分析与一人多贷不可做'。
> 这属于数据边界，不是实现缺陷 —— 我认为说清边界比硬凑一个维度更负责。"

---

> 📌 **配套文档**：`项目从零开始指导书.md` —— 本文档回答"做什么、为什么"，指导书回答"**具体怎么一步步做**"（含命令、代码、验收标准）。

---

## 一、开工前必须想清楚的六件事

### 1.1 这个项目到底在做什么（一句话）

> **把一份原始的信贷放款流水，加工成一套能回答"这笔贷款会不会逾期""哪批客户质量在恶化""风控策略改完有没有效果"的数据底座。**

**注意**：你做的是**数据工程/数仓**，不是机器学习建模。区别在于——
- ❌ 不是：训练一个模型预测违约（那是算法岗，你不投）
- ✅ 是：把数据**分层加工、建成指标、保证口径一致、能支撑分析**

面试官要看的是"你会不会建数仓、口径清不清楚"，而不是"你的 AUC 多少"。

### 1.2 为什么必须选信贷方向

| 对比 | 通用项目（如电商订单） | **信贷项目** |
|---|---|---|
| 面试相关性 | 🟡 "练手项目" | ✅ **"相关经验"** |
| 面试官熟悉度 | 中 | ✅ 高（银行面试官天天看这些指标） |
| 追问深度 | 浅 | ✅ 能追到"逾期口径怎么定的" |

**同样的工作量，但一个是练习、一个是履历。**

### 1.3 你的起点与终点

| 项目 | 起点（2026.11） | 终点（2027.01中） |
|---|---|---|
| SQL | 窗口函数熟练 | 能写拉链/快照/增量加工 + 分区表 |
| Python | 能看懂 | 能独立写清洗、指标、校验脚本 |
| 数仓 | 无概念 | 能画分层架构、说清建模理由 |
| **Linux/工程** | 无 | 能在 Linux 下跑流程 + Git 管理 + 任务调度 |
| 项目经历 | **空白** | ✅ **一段可讲 15 分钟的项目 + GitHub 仓库** |

### 1.4 ⭐ v1.1 新增：三条铁律升级为五条

| # | 铁律 | 原因 |
|---|---|---|
| 1 | **先跑通最小闭环，再优化** | 新手最大失败模式是"设计太完美，永远跑不起来" |
| 2 | **每一层都要能验证** | 数仓的核心是**数据可信**，不是代码漂亮 |
| 3 | **从第一天就写文档** | 最后再补文档 → 一定补不出来 → 面试讲不清 |
| 4 | 🆕 **从第一天就进 Git** | 没有版本管理的项目 = 面试官无法验证的作品集 |
| 5 | 🆕 **每个产出物都要有数字** | "行数 X 万、耗时 Y 分、一致率 100%" —— 没有数字就没有说服力 |

### 1.5 ⚠️ 最重要的一条：避免"数据泄漏"

信贷数据里有个经典陷阱：**很多字段是"放款后才知道的"**（如已还期数、最后还款状态、回收金额）。

> **如果把这些字段当成"放款时的信息"使用，指标就完全错了。**

**做法**：在设计阶段就明确划分"申请时点信息" vs "表现期信息"，并在文档里写清楚（见 §3.3）。

这一条如果你能在面试中主动讲出来，**面试官会立刻判断你"懂业务"**——这是区分"学生项目"和"真实工程思维"的分水岭。

> ⚠️ **v1.1 提醒**：这个亮点在 v1.0 的简历段落里**完全没体现**，等于白做。v1.1 已把它写进简历模板（§7.2）。

### 1.6 🆕 第二条同等重要：认清数据的边界（v1.1 新增）

你下载的是 `accepted_2007_to_2018Q4.csv.gz` —— **只包含"已批准并放款"的贷款**。这带来三个必须承认的边界：

| 边界 | 后果 | 正确做法 |
|---|---|---|
| **无拒绝样本** | "批准率""通过率"**算不出来**（分母不存在） | **删除此类指标**，不写进简历 |
| **幸存者偏差** | 你观测的是"被批准的人"的表现，不代表全客群 | 在文档和面试中**主动说明影响范围** |
| **无政策/宏观数据** | 无法证明"逾期上升是因为策略放松" | 只陈述**数据内可验证**的结论（见 §6.2） |

> 🔑 **主动说出"我这份数据有什么做不到"，比编一个漂亮结论更能体现专业度。**
> 面试官听到"我识别到数据集仅含放款样本，因此剔除了批准率类指标" —— 这一句的含金量高于任何一张图。

---

## 二、第一步：选数据集与确认边界（半天）

### 2.1 数据集对比

| 数据集 | 获取方式 | 规模 | 适配度 | 说明 |
|---|---|---|---|---|
| **LendingClub 贷款数据** | Kaggle 搜索 "LendingClub" | **2,260,700 行**、151 字段（本地实测） | ⭐⭐⭐⭐⭐ **首选** | 字段丰富，足够建数仓与算逾期指标 |
| **LendingClub 拒绝样本** | Kaggle（`rejected_*.csv`） | 2700 万行、9 字段 | ⭐⭐⭐ | 🆕 **可选补充**：能算批准率的近似值，但字段极少、无表现期 |
| **Give Me Some Credit** | Kaggle | 15 万行、11 字段 | ⭐⭐⭐ | 字段少，**不适合做数仓** |
| **Home Credit Default Risk** | Kaggle | 多表关联、规模大 | ⭐⭐⭐⭐ | 多表结构更接近真实数仓，**进阶可选** |

### 2.2 推荐方案

> **主力：`accepted_2007_to_2018Q4`（已下载）。**

**关于拒绝样本（v1.1 新增判断）**：
- ✅ **值得做**：把 `rejected_*.csv` 作为**独立的"申请侧数据"**引入，只用其 `amount` / `application_type` / `risk_score` / `date` 做**申请量趋势**与**申请结构对比**
- ❌ **不要做**：不要强行把两份数据 JOIN 起来算"批准率" —— 拒绝样本**没有唯一客户 ID、没有地区、没有收入**，JOIN 键对不上，硬做出来的批准率是错的
- ✅ **正确表述**：在文档中写"拒绝样本字段不足以支撑与放款样本的关联，因此批准率以**申请侧独立口径**呈现，不与放款指标混算"

> 🎯 这个处理方式本身就是面试素材：**它证明你知道"数据能不能 JOIN 要看键和粒度"，而不是拿到两张表就往一起拼。**

### 2.3 数据合规提醒

- 这些是**公开的脱敏数据集**，可以用于学习与作品集
- 简历与文档中**不要声称**这是"某银行真实数据"
- 表述应为"基于公开信贷数据集"——**诚实反而加分**
- GitHub 仓库中**不要提交原始数据**（`.gitignore` 排除 `data/`）

---

## 三、第二步：数据探查与数仓设计（约 20 小时）

> **在动手建表之前，必须先摸清数据长什么样。** 跳过这一步，后面必然返工。

### 3.1 数据探查（EDA）要产出的东西

一份 **《数据探查报告》**（Markdown，4–6 页）：

| 板块 | 内容 |
|---|---|
| 数据概览 | 行数、列数、字段清单与含义 |
| 主键检查 | 是否有唯一键？是否重复？ |
| **字段分类** | 申请时点信息 / 表现期信息 / 标签（**为 §1.5 防泄漏做准备**） |
| 缺失值分布 | 每个字段缺失率，标出 >50% 的字段 |
| 取值分布 | 关键字段枚举值（如 `loan_status` 有哪些取值） |
| 异常值 | 金额为负、日期越界、年龄不合理等 |
| 时间跨度 | 最早/最晚放款日期，判断能否做趋势分析 |
| 🆕 **行数与体量实测** | 真实行数、裁剪后内存占用 —— 用于简历数字与引擎选择论证 |

**必查项（具体做法见指导书 §3）**：

```sql
-- ① 主键唯一性
SELECT id, COUNT(*) FROM ods_loan_raw GROUP BY id HAVING COUNT(*) > 1;

-- ② 关键字段枚举（决定你的指标口径）
SELECT loan_status, COUNT(*) FROM ods_loan_raw GROUP BY loan_status ORDER BY 2 DESC;

-- ③ 时间跨度
SELECT MIN(issue_d), MAX(issue_d) FROM ods_loan_raw;
```

> ⚠️ LendingClub 的 `loan_status` 取值很多（Fully Paid / Charged Off / Current / Late (31-120 days) / In Grace Period…）。
> **"什么算逾期"完全取决于你怎么归类这些取值**——这就是"口径"，面试必问。

✅ **产出物**：`01_eda/数据探查报告.md`

### 3.2 分层架构设计

```
┌─────────────────────────────────────────────────┐
│  ADS  应用层   ← 直接给分析/报表用的指标表        │
│        如：逾期率月报、Vintage 矩阵、迁徙率矩阵    │
├─────────────────────────────────────────────────┤
│  DWS  汇总层   ← 按主题做轻度汇总（宽表）          │
│        如：月末贷款快照表、客户级汇总、月度汇总     │
├─────────────────────────────────────────────────┤
│  DWD  明细层   ← 清洗后的明细，一行一业务事实      │
│        如：贷款事实表、客户维表、产品维表          │
├─────────────────────────────────────────────────┤
│  ODS  原始层   ← 原样落地，不做加工（便于回溯）    │
│        如：原始放款流水                            │
└─────────────────────────────────────────────────┘
```

| 层 | 存在意义 | 一句话解释（面试要能说） |
|---|---|---|
| **ODS** | 数据可回溯 | "原始数据原样保存，出问题时能查源头" |
| **DWD** | 清洗 + 规范化 | "统一口径、处理脏数据，形成一行一事实的明细" |
| **DWS** | 减少重复计算 | "按主题预汇总，避免每次分析都从明细算" |
| **ADS** | 面向应用 | "直接产出报表/看板需要的指标表" |

### 3.3 维度建模设计

**要设计的表（v1.1 修订版）**：

| 表类型 | 表名 | 说明 | 粒度 | v1.1 变化 |
|---|---|---|---|---|
| **事实表** | `dwd_loan_fact` | 贷款放款事实（**申请时点口径**） | 一行 = 一笔贷款 | — |
| **事实表** | `dwd_loan_perf_fact` | 贷款表现事实（**表现期末口径**） | 一行 = 一笔贷款的最新表现 | 🆕 与申请时点**物理分离**，从结构上杜绝泄漏 |
| **快照事实表** | `dws_loan_snapshot_m` | 月末贷款状态快照 | 一行 = 一笔贷款 × 一个观测月末 | 🔄 **替代 v1.0 的 SCD2 拉链表** |
| **维度表** | `dim_applicant` | 🆕 申请人画像维（年收入、地区、FICO 等申请时点属性） | 一行 = 一笔贷款 | 🔄 **替代 v1.0 的 `dim_customer`**（`member_id` 整列为空，无客户识别键） |
| **维度表** | `dim_product` | 产品维（期限、利率档、评级） | 一行 = 一个产品组合 | — |
| **维度表** | `dim_date` | 日期维（年月、季度、星期） | 一行 = 一天 | — |

**必须做的设计决策（面试会问）**：

| 决策点 | 你要给出的答案 |
|---|---|
| 星型 vs 雪花 | **星型**（维度不进一步范式化，查询更快、更易理解） |
| 事实表类型 | **事务事实表**（每笔贷款）+ 🆕 **周期快照事实表**（月末状态） |
| 是否要拉链表 | 🔄 **本项目用周期快照，不用 SCD2** —— 理由见 §3.4 |
| 分区策略 | 按 **放款月份** 分区（便于增量与历史回溯） |

### 3.4 🔄 v1.1 重大修订：用"月末快照事实表"替代 SCD2 拉链表

**v1.0 的做法及问题**：
> v1.0 要建 `dim_customer_scd2`，记录客户评级/收入的历史变化。但 LendingClub 是**单次快照数据**，`annual_inc`、`grade` 都是**放款时点值**，没有历史切片。你无法构造真实的"属性变更"序列，只能人为造假数据来演示 —— 一旦被问"你的 SCD2 数据从哪来的"，杀伤力极大（拉链表瞬间从"懂数仓"变成"背概念"）。

**v1.1 的做法：月末快照事实表（Periodic Snapshot Fact Table）**

| 字段 | 说明 |
|---|---|
| `loan_id` | 贷款 ID |
| `snapshot_month` | 观测月末（如 `2019-01`） |
| `mob` | 账龄 = 观测月 − 放款月 |
| `out_prncp` | 期末未偿本金 |
| `total_rec_prncp` | 累计已收本金 |
| `dpd_bucket` | 该观测月末的逾期档位 |
| `is_delinquent` | 该月末是否逾期（0/1） |

**为什么它更好（面试话术）**：

> "我原本打算做客户属性拉链表，但发现这份数据是**放款时点的静态快照**，不存在真实的属性变更序列，硬做出来是假的。
> 我改成了**月末快照事实表**：按 `放款月 − 观测月` 的差值计算账龄 MOB，记录每笔贷款在每个观测月末的未偿余额和逾期档位。
> 它同样是**周期快照事实表**这一标准数仓模式，同样解决'历史状态如何追踪'的问题，但**数据是真实的、可校验的** —— 而且它天然支撑了 Vintage 与迁徙率分析。"

**附加价值**：月末快照表是 **Vintage（§4.2）和迁徙率（§4.3）的计算基础**，比拉链表更贴业务。

> ✅ **保留 SCD2 知识**：面试若问"拉链表怎么做"，你依然要能答（见 §7.4 面试话术）。但**项目里不放假数据**。

### 3.5 关键字段设计（防泄漏的落地）

**⭐ 物理分离原则（v1.1 强化）**：

| 字段类别 | 代表字段 | 存放位置 | 使用规则 |
|---|---|---|---|
| **申请时点信息** | `issue_d`、`loan_amnt`、`term`、`int_rate`、`grade`、`purpose`、`annual_inc`、`emp_length`、`home_ownership` | `dwd_loan_fact` + 维表 | ✅ **可用于风险分层、客群分析** |
| **表现期信息** | `loan_status`、`total_rec_prncp`、`total_rec_int`、`last_pymnt_d`、`out_prncp` | `dwd_loan_perf_fact`（**独立表**） | ⚠️ **只能用于计算标签与表现指标，禁止与申请时点字段同表同用** |
| **派生标签** | `is_bad`、`dpd_bucket` | `dwd_loan_perf_fact` → ADS | 从表现期派生 |

> 🔑 **v1.0 说"标注为表现期"，v1.1 要求"物理分表"** —— 从"我记得别用错"升级为"结构上不可能用错"。这是面试中很有力的表述：**"我不是靠自觉，是靠表结构约束。"**

### 3.6 产出物

✅ **`01_eda/数据探查报告.md`**
✅ **`02_design/数仓设计文档.md`**，包含：
- 分层架构图（**一定要画图**）
- 表清单（表名、粒度、字段、分区）
- 星型模型图
- **字段分类表**（申请时点 / 表现期）
- **口径定义表**（见 §4）
- 关键设计决策与理由（含"为什么不用 SCD2"）

---

## 四、第三步：定义指标体系与口径（约 8 小时）—— ⭐ 银行最看重

> **银行数据岗的核心竞争力是"口径清楚"。** 这一步的产出，是你面试时最能体现业务理解的部分。

### 4.1 指标口径表（v1.1 修订：删除批准率）

| # | 指标 | 定义 | 计算逻辑 | 备注 |
|---|---|---|---|---|
| 1 | **放款金额 / 笔数** | 统计期内成功放款的总额/笔数 | `SUM(loan_amnt)` / `COUNT(1)` | — |
| 2 | **不良率（逾期率）** | ⚠️ 必须明确"什么算不良" | `loan_status IN ('Charged Off','Default')` 计为不良 | 口径见 §4.4 |
| 3 | **逾期天数分档（DPD bucket）** | 按逾期天数分档 | Current / 1-30 / 31-60 / 61-90 / 90+ | — |
| 4 | **平均借款金额** | 总金额 / 笔数 | `SUM/COUNT` | — |
| 5 | **加权平均利率** | 按金额加权（**不是简单平均**） | `SUM(loan_amnt*int_rate)/SUM(loan_amnt)` | 面试常考"为什么加权" |
| 6 | **Vintage 逾期率** | ⭐ 按放款月份分组，看账龄 MOB 维度 | 见 §4.2 | 风控核心方法 |
| 7 | **迁徙率（Roll Rate）** | 从上期档位迁徙到下期档位的比例 | 见 §4.3 | — |
| 8 | 🆕 **期末未偿余额（EAD）** | 观测月末的未偿本金合计 | `SUM(out_prncp)` by 月末 | 银行核心口径之一 |
| ~~9~~ | ~~**批准率**~~ | ❌ **v1.1 删除** | 拒绝样本无关联键，分母不成立 | 见 §1.6 |

### 4.2 ⭐ Vintage 分析（风控核心方法）

**概念**：把**同一放款月份**的贷款作为一批（vintage），观察它们在**不同账龄（MOB, Month on Book）**下的表现。

**为什么要这么做**（面试必答）：
> 不同月份放款的政策不同、客群不同。**直接比较不同月份的总逾期率会误导**——因为老批次账龄长、暴露更充分。Vintage 分析把账龄对齐，才能公平比较"哪批客户质量更差"。

**表结构**：

| 放款月 | MOB1 逾期率 | MOB3 | MOB6 | MOB12 |
|---|---|---|---|---|
| 2018-01 | 0.8% | 1.5% | 2.3% | 3.1% |
| 2018-02 | 0.9% | 1.8% | … | … |

**计算思路**：
```
账龄 MOB = 观测月 − 放款月
按 放款月 + MOB 分组，统计该组内不良笔数 / 总笔数
在 dws_loan_snapshot_m 上做（每月快照 → 可精确算每个 MOB 的逾期状态）
```

### 4.3 迁徙率分析（Roll Rate）

**概念**：本期处于某档位的贷款，下期迁徙到各档位的比例。

| 期初档位 | → Current | → 1-30 | → 31-60 | → 核销 |
|---|---|---|---|---|
| Current | 95% | 4% | 0.8% | 0.2% |
| 1-30 | 60% | 25% | 12% | 3% |
| 31-60 | 30% | 20% | 35% | 15% |

**用途**：预测未来不良、评估催收效果。

**实现要点**：在 `dws_loan_snapshot_m` 上做**自关联（self-join）**，连接条件为 `本月.loan_id = 下月.loan_id AND 下月.snapshot_month = 本月.snapshot_month + 1个月`。

> ⚠️ **诚实提醒**：迁徙率严格意义上需要**逐月**的逾期状态。LendingClub 的 `loan_status` 只是**单一最终状态**，无法还原真实的历史逐月迁徙。**v1.1 的正确做法**：
> - 方案 A（推荐）：基于 `snapshot_month` 用**规则推导**状态（如按 `last_pymnt_d` 距今月数推算 DPD 档位），并在文档中**明确标注为"规则推导，非原始逐月状态"**
> - 方案 B：只做"最终状态分布迁移"（放款时 `grade` 档 → 最终 `loan_status`），更保守但 100% 真实
>
> 🔑 **主动标注推导假设 = 加分；假装有逐月数据 = 减分。**

### 4.4 口径表模板（照此填写）

```
| 指标名 | 业务含义 | 计算公式 | 数据来源表 | 口径说明 | 负责人 |
|--------|---------|---------|-----------|---------|--------|
| 不良率 | 不良贷款笔数占比 | 不良笔数/总笔数 | dwd_loan_perf_fact | 不良 = Charged Off + Default；不含 Current；按放款月口径统计 | — |
| 期末未偿余额 | 观测月末未偿本金合计 | SUM(out_prncp) | dws_loan_snapshot_m | 仅统计期末状态为 Current/Late 的贷款 | — |
```

> 🔑 **这份"口径表"是银行数据岗最真实的日常工作产物。** 面试时拿出来讲，说服力极强。

### 4.5 产出物

✅ **`03_metrics/指标口径字典.md`**（8 项指标，每项含业务含义/公式/来源/口径说明）

### 4.6 🆕 数据边界与偏差说明（独立章节，v1.1 新增）

> 单独成章，**不是脚注**。这份说明直接复用为面试话术。

| 边界项 | 具体限制 | 对指标的影响 | 处理方式 |
|---|---|---|---|
| **仅含放款样本** | 无拒绝客群 | 全部风险指标存在**幸存者偏差**；批准率不可计算 | 删除批准率；所有风险结论限定在"已放款客群"内 |
| **拒绝样本不可关联** | 无客户 ID / 地区 / 收入 | 无法做通过率漏斗 | 作为**独立申请侧数据集**单独分析申请量趋势 |
| **状态为最终快照** | 无逐月状态历史 | 迁徙率需规则推导 | 标注推导假设，或改用最终状态分布迁移 |
| **无政策/宏观数据** | 无准入规则、无经济指标 | 无法归因"为什么风险变化" | 只陈述数据内可验证事实（§6.2） |
| **样本期截至 2018Q4** | 晚批次账龄不足 | 近期 Vintage 的 MOB12+ 不可比 | Vintage 矩阵按**可用 MOB** 截断，不补全 |

✅ **产出物**：`03_metrics/数据边界与偏差说明.md`

---

## 五、🆕 第四步：工程基线（约 20 小时）—— v1.1 全新章节

> ⭐ **这是 v1.1 相对 v1.0 最大的增量。** v1.0 把这四项（Linux、Git、调度、增量）全部漏掉，而它们在银行数仓 JD 中出现频率极高。
> **不做这一章，你的简历技术栈画像会是"数据分析"，而不是"数据开发"。**

### 5.1 环境基线：Linux（Docker 优先）

**为什么必须**：银行数仓岗 JD 几乎必含 Linux/Shell；Hive/Spark 也运行在 Linux 上，这是阶段三的前置。

**⚠️ 环境现实（本地实测）**：
- ✅ Docker 29.2.1 已可用
- ❌ WSL 需要管理员权限（本机 `wsl --status` 返回 `E_ACCESSDENIED`）

**因此推荐路线（按优先级）**：

| 方案 | 做法 | 优点 | 缺点 |
|---|---|---|---|
| **A（推荐）** | **Docker + Ubuntu 容器** + 挂载项目目录 | 无需管理员权限，环境干净可复现 | 需要理解 volume 挂载 |
| B | 申请管理员权限装 WSL2 + Ubuntu | 体验最接近真实服务器 | 需要权限 |
| C | 云服务器（学生机 ¥10/月） | 最接近生产，可跑 Hive | 要花钱；**阶段三再用更划算** |

**验收标准**：能在 Linux 下用 Shell 脚本串起"读原始数据 → 跑 Python 清洗 → 输出结果"三步。

### 5.2 Git 与作品集基线

| 要求 | 具体做法 |
|---|---|
| 公开仓库 | GitHub 建 public repo，命名如 `credit-dwh` |
| **`.gitignore`** | ⭐ **必须排除 `data/`**（原始数据 374MB，不能提交）；排除 `__pycache__`、`.ipynb_checkpoints`、`*.log` |
| 提交规范 | 用 `feat:` / `fix:` / `docs:` / `refactor:` 前缀，**每天至少一次有意义提交** |
| 大文件 | 处理后的数据不入库；如需共享，说明生成方式让对方自己跑 |
| README | 含架构图、目录结构、如何运行、核心结论 |
| 脱敏检查 | 提交前确认无本地绝对路径、无用户名、无 token |

> 🔑 **面试时"能打开链接看代码"和"只有一段文字描述"，是两个量级。**

### 5.3 任务调度（银行数仓日常工作的核心）

**为什么必须**：数仓开发岗每天在做的事就是"配任务、看依赖、处理失败重试"。**不会调度 = 不会数仓开发。**

**工具选择**：

| 工具 | 推荐度 | 理由 |
|---|---|---|
| **Apache DolphinScheduler** | ⭐⭐⭐⭐⭐ **首选** | 国产、银行/券商大量使用、单机 Docker 一键起、可视化 DAG |
| Apache Airflow | ⭐⭐⭐⭐ | 互联网主流，Python 定义 DAG |
| Cron + Shell | ⭐⭐ | 最简，但**没有依赖管理和重试**，不能替代 |

**要做的事**：

| 任务 | 内容 |
|---|---|
| 建 DAG | 把 `ods_load → dwd_clean → dwd_derived → dws_snapshot → ads_metrics → dq_check` 串成**有依赖的流水线** |
| 配失败重试 | 每个任务配重试次数 + 重试间隔 |
| 配告警 | 失败时邮件/钉钉通知（至少配一个） |
| 参数化 | 用 `biz_date` 参数支持**按日期重跑** |
| 记录耗时 | 记录每个任务耗时 → **这就是简历里的数字来源** |

**验收标准**：一键触发全流程，中途手动 kill 一个任务，能自动重试并最终成功。

### 5.4 增量加工（阶段二就用 SQL 实现，不等 Hive）

**为什么在阶段二做**：这是面试必问"全量 vs 增量"，而**逻辑与引擎无关** —— 用 MySQL 分区表一样能讲清楚。

| 概念 | 落地方式（阶段二） |
|---|---|
| **分区** | MySQL 按 `issue_month` 做**分区表**（或至少建索引 + 分区键列）；若用 PostgreSQL 用原生分区 |
| **增量识别** | 以 `issue_d` 为水位（watermark）：只处理 `issue_d > 上次水位` 的数据 |
| **水位管理** | 建 `etl_watermark` 表记录每次跑的 `max(issue_d)` 与时间戳 |
| **幂等** | 重跑同一天不产生重复数据（先删后插 / `UPSERT` / `MERGE`） |
| **回补（backfill）** | 支持指定 `biz_date` 重跑历史分区 |

**面试话术**：
> "我把 ODS→DWD 做成按放款月分区的增量加工，用水位表记录上次处理到哪个月，重跑时先按分区删除再插入保证幂等。全量重跑一次约 X 分钟，增量只需 Y 秒（当月数据量小）。"

### 5.5 数据质量校验（代码化，不只是文档）

v1.0 把校验规则写在文档里 —— **v1.1 要求写成可执行的断言脚本**。

| 校验项 | 规则 | 实现方式 | 严重级别 |
|---|---|---|---|
| **行数守恒** | ODS 行数 ≥ DWD 行数（差值需能解释） | 计数比对 + 差异原因登记 | ERROR |
| **主键唯一** | `dwd_loan_fact.id` 唯一 | `GROUP BY ... HAVING COUNT(*)>1` = 0 | ERROR |
| **汇总一致性** ⭐ | DWS/ADS 汇总值 = DWD 明细重算值 | 双向重算比对 | ERROR |
| **金额非负** | `loan_amnt >= 0` | 条件计数 = 0 | ERROR |
| **枚举合法** | `loan_status` 在允许集合内 | `NOT IN (...)` 计数 = 0 | ERROR |
| **时间合理** | `issue_d <= 当前日期` | 条件计数 = 0 | WARN |
| 🆕 **分区完整性** | 每个放款月分区都有数据，无空洞 | 月份序列比对 | WARN |
| 🆕 **无未来信息** ⭐ | `dwd_loan_fact` 中**不含**任何表现期字段 | **字段名黑名单校验** | ERROR |

> 🔑 **最后一条是 v1.1 的杀手锏**：用**自动化校验**保证"申请时点表里不可能混进表现期字段"。面试时讲出来，等于告诉面试官"我把数据治理做成了机制，不是靠自觉"。

✅ **产出物**：`src/dq_check.py` + `05_quality/数据质量校验报告.md`

### 5.6 本步产出物小结

✅ Linux（Docker）可运行环境 + 3 个 Shell 脚本
✅ GitHub 公开仓库 + 规范提交历史
✅ DolphinScheduler DAG（含重试与告警）
✅ 增量加工（水位表 + 幂等 + 回补）
✅ 可执行的 8 项数据质量校验脚本

---

## 六、第五步：实现（Python + SQL v1）（约 30 小时）

### 6.1 项目目录结构

```
credit-dwh/
├── README.md                    # 项目说明（最重要）
├── .gitignore                   # ⭐ 必须排除 data/
├── requirements.txt
├── docker-compose.yml           # 🆕 MySQL + Ubuntu 容器
├── 01_eda/
│   ├── 数据探查报告.md
│   ├── 字段画像.csv             # 每个字段的缺失率/类型/样例
│   └── key_checks_output.txt    # 主键/枚举/时间跨度 实测输出
├── 02_design/
│   └── 数仓设计文档.md          # 含 Mermaid 架构图与星型模型图
├── 03_metrics/
│   ├── 指标口径字典.md
│   └── 数据边界与偏差说明.md
├── 04_analysis/
│   ├── 分析报告.md
│   └── images/                  # 4 张图（趋势/Vintage/风险分层/迁徙率）
├── 05_quality/
│   └── 数据质量校验报告.md
├── 06_ops/                      # 🆕 v1.1 新增
│   ├── dags/                    # DolphinScheduler 工作流定义
│   ├── shell/                   # Shell 脚本（run_ods.sh / run_dwd.sh ...）
│   ├── logs/                    # ❌ 不入 git
│   └── runbook.md               # 运维手册（失败怎么处理）
├── data/                        # ❌ 不入 git
│   ├── raw/                     # 原始 gz 文件（374MB，绝不提交）
│   ├── processed/               # 加工中间结果
│   └── mysql_init/              # 建库初始化脚本
├── src/
│   ├── __init__.py              # ⭐ 必须有，否则 import src 可能失败
│   ├── config.py                # 🆕 路径/DB/口径常量（口径字典的代码化）
│   ├── ods_load.py              # ODS：原始数据落地
│   ├── dwd_clean.py             # DWD：清洗、标准化
│   ├── dwd_derived.py           # DWD：派生字段（is_bad/dpd_bucket/mob）
│   ├── dim_build.py             # 维度表构建
│   ├── dws_snapshot.py          # DWS：月末快照
│   ├── ads_metrics.py           # ADS：指标计算（Python 侧，用于交叉校验）
│   ├── dq_check.py              # 🆕 质量校验（8 项规则，代码化）
│   ├── watermark.py             # 🆕 增量水位管理
│   └── utils.py                 # 公共函数（DB 连接、日志、读原始文件）
└── sql/
    ├── ddl/                     # 建表语句
    ├── dwd_loan_fact.sql
    ├── dwd_loan_perf_fact.sql
    ├── dws_loan_snapshot_m.sql
    ├── ads_vintage.sql
    ├── ads_roll_rate.sql
    └── dq/                      # 质量校验 SQL（check_no_leakage.sql 等）
```

> 🔑 **有个像样的目录结构本身就是加分项** —— 它说明你有工程习惯。
> 📌 **运行方式约定**：所有脚本用 `python -m src.xxx` 从项目根执行（原因见指导书 §0.8）。

### 6.2 分步实现（按顺序，每步都要能验证）

| 步 | 任务 | 产出 | 验证方式 |
|---|---|---|---|
| 1 | **ODS 落地**：读入、统一命名、加装载时间戳 | `ods_loan_raw` | 行数与原始文件一致 |
| 2 | **DWD 清洗**：类型转换（`int_rate` 去 `%`）、日期解析（`Dec-2018`）、缺失处理、去重、异常标记 | `dwd_loan_fact` | 主键唯一、日期范围合理 |
| 3 | **DWD 派生**：`is_bad`、`dpd_bucket`、`mob`、放款年月 | `dwd_loan_perf_fact` | 分布合理，无 NULL 异常 |
| 4 | **维度表**：`dim_applicant`、`dim_product`、`dim_date` | 维表 | 维表主键唯一，能与事实表 JOIN；`dim_applicant` 行数 = DWD 行数 |
| 5 | 🆕 **月末快照**：按观测月末生成贷款状态快照 | `dws_loan_snapshot_m` | 每笔贷款 × 每月一行，MOB 连续无跳变 |
| 6 | **DWS 汇总**：月度放款汇总、客户级汇总、产品级汇总 | `dws_*` | 汇总数 = 明细数（**关键校验**） |
| 7 | **ADS 指标**：逾期率月报、Vintage 矩阵、迁徙率矩阵、期末未偿余额 | `ads_*` | 与手算抽样核对 |
| 8 | 🆕 **增量与幂等**：水位表 + 按分区重跑 | 可重跑流水线 | 同一天重跑两次，结果完全一致 |
| 9 | 🆕 **质量校验**：8 项规则代码化 | 校验脚本 + 报告 | 全部通过（或差异被登记解释） |

### 6.3 常见技术坑与解法

| 坑 | 症状 | 解法 |
|---|---|---|
| **内存不足** | 读 374MB gz 时进程被杀 | 分块读取（`chunksize`）或先裁剪列；只保留需要的 ~40 列 |
| **日期格式混乱** | `issue_d` 是 `"Dec-2018"` | **显式指定格式** `%b-%Y` 解析，不靠自动推断 |
| **利率是字符串** | `"13.56%"` 无法计算 | 去 `%` 转 float |
| **缺失值策略不清** | 到处填 0 导致指标失真 | **按字段分别决策并在文档记录** |
| **JOIN 后行数变多** | 维表有重复键 | JOIN 前检查维表主键唯一性 |
| **口径前后不一** | 不同脚本算法不同 | **统一从口径字典取定义** |
| 🆕 **时区/月份边界** | 月末快照跨月错位 | 统一用 `date` 类型 + 自然月，禁止用"30天"近似 |
| 🆕 **中文字符集** | MySQL 存中文乱码 | 建库建表统一 `utf8mb4` |

### 6.4 产出物

✅ 可运行的 `src/` 脚本 + `sql/` 脚本 + 处理后的数据
✅ `06_ops/` 调度与 Shell 脚本
✅ **`README.md`**（含架构图、运行方式、结论摘要、量化数字）

---

## 七、第六步：分析产出与项目包装（约 14 小时）

### 7.1 建议做 4 个分析

| 分析 | 内容 | 图表 |
|---|---|---|
| **1. 放款趋势** | 按月放款金额/笔数，看规模变化 | 折线图 |
| **2. Vintage 逾期率** | 不同放款月 × 账龄的逾期率矩阵 | 热力图 |
| **3. 风险分层** | 不同信用评级/利率档的逾期率差异 | 柱状图 |
| **4. 客户画像** | 不同收入/职业/地区的借款分布与风险差异 | 分组柱状图 |

### 7.2 ⚠️ 每个分析的解读方式（v1.1 修订：消除过度归因）

**v1.0 的错误示范**：
> ❌ "2018 年逾期率上升，从 Vintage 表看主要是 2018-Q2 批次在 MOB6 后表现恶化，**可能与当期客群下沉或授信政策放松有关，建议核查该期准入规则**"

**问题**：后半句在这份数据里**无法验证** —— 你既没有准入规则、没有拒绝样本、也没有宏观政策数据。面试官会立刻反问："你怎么证明是政策放松？" 一旦答不上来，反而暴露"为了讲故事而编因果"。

**v1.1 的正确写法（两段式）**：

> ✅ **第一段：数据内可验证的事实**
> "2018-Q2 放款批次在 MOB6 的逾期率为 2.8%，较 2017 年同期批次（1.9%）高 0.9 个百分点。
> 从**本数据可验证的口径**看，该批次结构发生变化：`grade` C–E 段占比从 41% 升至 52%，加权平均利率从 12.3% 升至 14.1%，平均年收入下降 X%。**风险敞口确实向次级客群迁移。**"
>
> ✅ **第二段：明确边界**
> "至于迁移的驱动因素是准入策略调整、客群自然变化还是外部环境，**当前数据无法区分** —— 需要补充申请侧全量数据、拒绝样本与准入规则变更记录才能归因。"

> 🔑 **"明确说出我不能证明什么"比"给出一个漂亮因果"更像专业人。** 这一句本身就是面试的加分项。

### 7.3 简历上怎么写

#### 版本 A：阶段二完成即可用（v1.1 修订稿）

```
金融信贷离线数仓（个人项目，GitHub 开源）｜2026.11–2027.01
· 基于 LendingClub 公开信贷数据（2,260,700 笔贷款 / 151 字段，实测），设计并实现
  ODS-DWD-DWS-ADS 四层数仓，产出申请时点事实表 + 表现期事实表 + 月末快照事实表
  + 3 张维表 + 4 张 ADS 指标表 + 8 项指标口径字典
· 在 Linux(Docker) 环境用 Python + SQL 完成清洗与指标加工，按放款月分区实现增量跑批
  （水位表 + 幂等重跑，重跑结果完全一致），用 DolphinScheduler 编排 6 任务依赖 DAG，
  含失败重试与超时告警
· 主动划分"申请时点信息/表现期信息"并做物理分表，通过字段黑名单校验自动阻断数据泄漏；
  识别数据集仅含放款样本的幸存者偏差与 member_id 整列缺失的限制，剔除批准率类指标、
  将客户维降级为申请人画像维，并说明影响边界
· 实现 8 项数据质量校验的可执行框架：行数守恒（ODS 2,260,700 → DWD 2,260,668，
  32 行缺放款日期已登记）、主键唯一、汇总一致性（ADS 与 DWD 明细重算一致率 100%）、
  枚举合法、分区完整性、无未来信息
· 完成 Vintage 逾期率与迁徙率分析，定位某放款批次 MOB6 不良率高于同期提前期批次
  __ 个百分点，风险敞口向 C–E 评级段迁移（占比 __%→__%）
```

#### 版本 B：阶段三完成后（v1.1 重定义，见 §八）

```
· 在上述 v1 基础上完成单机→分布式引擎迁移：Hive 分区表 + PySpark 加工，同一套口径
  双引擎实现，产出性能基准（全量 X min→Y min，数据倾斜场景优化前后对比）
· 定位并解决 group-by 热点 key 导致的长尾 task（占比 30%），采用加盐打散两阶段聚合，
  任务耗时下降 Z%
· 完成分区裁剪、小文件合并、ORC/Parquet 选型对比，输出引擎选型结论
```

#### 🔑 简历三条硬规则（v1.1 新增）

1. **所有数字必须实测** —— 行数、耗时、一致率，**写前必须自己跑一遍**
2. **不写做不到的** —— 面试会验证每一个词
3. **必须放 GitHub 链接** —— 从"我说我会"到"你自己看"

### 7.4 面试怎么讲（三层递进 + v1.1 新增第四层）

| 层次 | 内容 | 时长 |
|---|---|---|
| **层次 1：做了什么** | 四层数仓、事实表+维表、指标口径 | 3 分钟 |
| **层次 2：为什么这么做** | 分层为了解耦复用；ODS 保留原始便于回溯；快照表支撑 Vintage | 5 分钟 |
| **层次 3：遇到什么问题、怎么解决** ⭐ | JOIN 行数膨胀 → 维表主键重复 → 加去重；字段缺失 60% → 不入指标并在文档标注；**申请时点 vs 表现期物理分表防泄漏** | 7 分钟 |
| **层次 4：🆕 我的判断与边界** ⭐⭐ | **为什么不用 SCD2 而用快照事实表**；**为什么删掉批准率**；**哪些结论我证明不了**；**为什么这个数据量其实不需要分布式，我选它是为了迁移验证** | 5 分钟 |

> 🔑 **层次 3 是分水岭，层次 4 是"学生"与"工程师"的真正分界。** 大多数学生只能讲层次 1，少数能讲层次 3，几乎没有学生能讲层次 4 —— 因为层次 4 要求你**对自己的项目有批判性判断**。

### 7.5 产出物

✅ **`04_analysis/分析报告.md`** + 图表（存 `04_analysis/images/`）
✅ **`README.md`**（最终版）
✅ 简历段落（版本 A）

---

## 八、第七步（阶段三，2027.02–03）：引擎迁移与性能基准 —— 🔄 v1.1 重定义

### 8.1 ⚠️ 为什么必须重定义

**v1.0 的阶段三是"把 v1 用 Hive/Spark 重写一遍"。**

**问题**：面试官会把它看成**重复劳动**（"同样的逻辑换个引擎跑一遍，学到了什么？"），而且**回答不了"为什么"** —— 为什么值得用分布式？性能提升了多少？选型依据是什么？

**v1.1 的定位**：

> **"同一套口径在单机与分布式两种引擎上的实现对比，含分区裁剪、数据倾斜处理与性能基准。"**

同一个工作量，但从"我学过 Hive"变成"**我能做出引擎选型判断**"。

### 8.2 升级内容

| 项 | v1（Python/SQL） | v2（Hive/Spark） | 要回答的问题 |
|---|---|---|---|
| 存储 | MySQL 分区表 | Hive 分区表（ORC/Parquet） | 列存 vs 行存的差异？ |
| 加工 | pandas / SQL | Hive SQL / PySpark | 同一逻辑两种实现差异？ |
| 分区 | 逻辑分区 | **物理分区 + 分区裁剪** | 分区裁剪带来多少提速？ |
| 增量 | 水位表 | **Hive 分区覆盖写 + 增量** | 增量比重跑快多少？ |
| 倾斜 | 未涉及 | ⭐ **识别并处理** | 怎么发现？怎么解决？ |
| 性能 | 无基准 | **双引擎基准对比** | 这个数据量该选哪个？ |

### 8.3 数据倾斜（面试高频追问）

**现象**：某个 key 的数据量远大于其他 key，导致个别 task 跑得极慢。

| 处理手段 | 适用场景 |
|---|---|
| `MAP JOIN`（小表广播） | 一大一小的表 JOIN |
| **加盐打散**（key 加随机前缀） | 大表 JOIN 大表且热点 key |
| **两阶段聚合** | GROUP BY 热点 key |
| 小文件合并 | 大量小文件 |

**面试表述模板**：
> "在按 `grade` 或客户维度聚合时，发现某个 key 的数据量占整体 30%，导致长尾 task 拖慢整个 stage。
> 我用加盐打散做两阶段聚合：先给热点 key 加随机前缀做局部聚合，再去掉前缀做全局聚合，
> 把该 stage 耗时从 X 分钟降到 Y 分钟。"

> ⚠️ **必须真做过才能这么讲。** 面试官会追问"加盐的随机前缀取几个？为什么？"

### 8.4 v2 产出

✅ Hive DDL（分区表）+ 加工 SQL + PySpark 脚本
✅ 数据倾斜处理说明（写入 README）
✅ **性能基准报告**：同一指标在单机 vs 分布式下的耗时、资源、结论
✅ 引擎选型结论（含"本项目这个数据量下我推荐哪个，为什么"）

---

## 九、时间表（可直接排进日历）

### 9.1 阶段二（2026.11 – 2027.01中，约 11 周）

> ⚠️ **相对 v1.0 延长约 3 周**：因为把 Linux/Git/调度/增量搬进来了。若时间不足，**优先砍 §4.3 迁徙率，不要砍 §五 工程基线**（工程基线是简历硬通货）。

| 周 | 任务 | 产出 |
|---|---|---|
| 0.5 | 环境搭建（Docker + Python + MySQL + Git 仓库） | 可运行环境 + GitHub 空仓库 |
| 1 | 选数据集 + EDA + 边界确认 | 数据探查报告 |
| 2 | 数仓设计（分层 + 模型 + 字段分类 + 画图） | 设计文档 + 架构图 + 星型图 |
| 3 | 指标口径定义 + 数据边界说明 | 口径字典 + 边界说明 |
| 4 | **ODS + DWD 实现**（清洗、派生、分表） | 清洗后明细 |
| 5 | 维表构建 + 月末快照表 | 维表 + `dws_loan_snapshot_m` |
| 6 | **DWS + ADS 实现**（含 SQL 版指标） | 指标表 |
| 7 | 🆕 **增量 + 幂等**（水位表） | 可重跑流水线 |
| 8 | 🆕 **DolphinScheduler 调度 + 质量校验代码化** | DAG + 校验报告 |
| 9 | 分析产出（4 个分析 + 图表）+ 解读改写 | 分析报告 |
| 10 | README + 简历包装 + 面试话术打磨 | ⭐ **项目 v1 完成** |

**关键检查点**：

| 时点 | 检查 | 未达标怎么办 |
|---|---|---|
| 第 1 周末 | 数据探查报告完成？**行列数实测了吗？** | 不要跳过，否则后面必返工 |
| 第 2 周末 | 架构图与星型图**画出来了吗？** | 画不出来说明没想清，先想清再动手 |
| 第 4 周末 | DWD 跑通且能验证？**申请时点/表现期分表了吗？** | 缩小字段范围，**先跑通再扩展** |
| 第 6 周末 | DWS/ADS 汇总与明细一致？ | 不一致先查 JOIN 与去重 |
| 第 8 周末 | **调度能一键跑通 + kill 后能重试成功？** | 这是 v1.1 的硬指标，不能省 |
| 第 10 周末 | README 与简历段落写好？**数字都是实测的？** | **没写完不算完成** |

### 9.2 阶段三（2027.02–03，约 6 周）

| 周 | 任务 |
|---|---|
| 1 | Linux/Shell 补强 + Hive 环境搭建（云服务器或 Docker） |
| 2–3 | Hive DDL 分区表 + 数据导入 + Hive SQL 复现核心指标 |
| 4 | **分区裁剪、小文件合并、ORC/Parquet 对比** |
| 5 | **数据倾斜复现与处理**（加盐两阶段聚合） |
| 6 | PySpark 重写关键链路 + **双引擎性能基准报告** + v2 文档 |

---

## 十、检查点与避坑

### 10.1 十一个常见失败模式（v1.1 新增三条）

| # | 失败模式 | 后果 | 对策 |
|---|---|---|---|
| 1 | **追求完美设计，迟迟不动手** | 永远没有产出 | **先跑通最小闭环** |
| 2 | **跳过 EDA 直接建表** | 后期大量返工 | EDA 必须做 |
| 3 | **不做数据质量校验** | 指标不可信，面试被问穿 | 至少做行数守恒+汇总一致 |
| 4 | **把表现期字段当申请时点用** | 指标逻辑错误（数据泄漏） | 🆕 **物理分表 + 字段黑名单校验** |
| 5 | **口径前后不一致** | 同一个指标两个数 | **统一从口径字典取** |
| 6 | **只用 Python 不写 SQL** | 银行要的是 SQL 能力 | SQL 脚本必须单独留档 |
| 7 | **陷入环境搭建** | 浪费数周 | 🆕 **Docker 优先（本机 WSL 不可用），DS 部署超 6 小时就降级为 Airflow/计划任务** |
| 8 | **不写文档** | 面试讲不清 | 从第一天就记 |
| 9 | 🆕 **不做版本管理** | 作品集无法验证；简历写了没链接 | **第一天就建 Git 仓库** |
| 10 | 🆕 **跳过任务调度** | 简历画像变"数据分析" | 阶段二必须补 DolphinScheduler |
| 11 | 🆕 **编造因果结论** | 面试被反问击穿 | **只讲数据内可验证的事实（§7.2）** |
| 12 | 🆕 **忽略文件尾部脏数据** | 页脚行混入数据，行数守恒校验失败 | 读取加 `skipfooter=1, engine="python"` |
| 13 | 🆕 **累计指标重复计数** | 累计不良率随 MOB 异常飙升 | 用 `MAX(...) OVER (ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW)` 取"曾不良"标志，**不要直接 SUM 逐月标记** |

### 10.2 ⚠️ 与保研的冲突提醒

> 本项目计划占用 **约 100 小时**（阶段二，含工程基线）。**若 10 月套磁发现推免名额 < 10，需切回加分路线，本项目应压缩或顺延。**

**压缩优先级（时间不足时按此顺序砍）**：

| 优先级 | 内容 | 可砍性 |
|---|---|---|
| 🔴 不可砍 | EDA、数仓设计、DWD/DWS/ADS 实现、口径字典、README | 砍了就不成项目 |
| 🟠 尽量保 | 工程基线（Git/调度/增量/质量校验代码化） | **简历硬通货，砍了画像失真** |
| 🟡 可砍 | 迁徙率分析、客户画像分析、第 4 张维表 | 减少分析数量 |
| 🟢 最先砍 | 拒绝样本接入、阶段三全部内容 | 可完全顺延到寒假后 |

---

## 十一、做完之后你会拥有什么

### 11.1 能力资产

| 资产 | 用途 |
|---|---|
| ✅ 一段**完整、可讲 15 分钟**的项目经历 | 简历"项目经历"从空到有 |
| ✅ **数仓建模 + 指标口径**能力 | 面试核心技术面 |
| ✅ SQL（窗口函数/快照/增量/分区）实战 | 笔面试硬通货 |
| ✅ **调度 + Linux + Git** 工程能力 | 🆕 银行数仓 JD 硬要求 |
| ✅ **数据质量校验 + 防泄漏机制化** | ⭐ 区分学生与工程思维 |
| ✅ 一个可放 GitHub 的作品集 | 投递时的额外证明 |
| ✅ **有数字、有基准、有判断**的项目叙述 | ⭐ 面试层次 4 的素材 |

### 11.2 🆕 量化指标清单（简历数字来源，必须实测填写）

> **简历里每一个 `X`，都必须在下面这张表里有对应实测值。** 空着的数字不允许写进简历。

| # | 指标 | 实测值 | 怎么测 |
|---|---|---|---|
| 1 | 原始数据行数 | **2,260,700** ✅已实测（文件 2,260,701 行 − 1 行页脚） | `gzip` 流式逐行统计 |
| 2 | 原始字段数 | **151** ✅已实测 | 表头逗号计数 |
| 3 | 保留字段数 | `______` 列 | 清洗后字段数 |
| 4 | 时间跨度 | **2007-06 至 2018-12** ✅已实测 | `MIN/MAX(issue_d)` |
| 5 | ODS→DWD 行数差异 | **32 行**（原因：`issue_d` 与 `loan_status` 同时为空） ✅已核实 | 行数守恒校验 |
| 6 | 汇总一致性一致率 | `______`%（目标 100%） | ADS vs DWD 重算 |
| 7 | 快照表行数 | `______` 行 | `COUNT(*)` |
| 8 | 全量跑批耗时 | `______` 分钟 | 调度任务日志 |
| 9 | 增量跑批耗时 | `______` 秒 | 调度任务日志 |
| 10 | 质量校验通过率 | `______` / 8 项 | 校验脚本输出 |
| 11 | DAG 任务数 | **6** ✅已定 | 调度界面 |
| 12 | 终态不良率（口径自检基准） | **≈19.98%** ✅已核实（269,360 / 1,348,099） | 若算出 ~11.9% 说明漏了 `is_terminal=1` |
| 13 | 🆕 v2 倾斜优化前后耗时 | `______` → `______` 分钟 | 阶段三基准报告 |

> 🔑 **一张填满的表 = 一份能过筛的简历。** 面试官问"你说的 2,260,700 是哪来的"，你要能立刻答出"我用 `gzip` 流式逐行统计的，文件总行数 2,260,701，减去 1 行页脚"。

### 11.3 一句话

> **这个项目的真正价值不是"我做过一个数仓"，而是"我能说清为什么这么建、口径怎么定、数据怎么保证可信、哪些结论我证明不了"。后者才是银行为什么要你。**

---

## 十二、附录：版本对照速查

| 维度 | v1.0 | **v1.1** |
|---|---|---|
| 阶段二周期 | 8 周 / 60–80h | **11 周 / 约 100h** |
| 阶段二技术栈 | Python(pandas) + SQL | **+ Linux + Git + DolphinScheduler + 增量** |
| 指标数量 | 8 项（含批准率） | **8 项（删批准率，加期末未偿余额）** |
| 历史追踪方案 | SCD2 拉链表（需造假数据） | **月末快照事实表（真实数据）** |
| 防泄漏手段 | 文档标注 | **物理分表 + 自动化黑名单校验** |
| 数据边界 | 脚注说明 | **独立章节 + 面试话术** |
| 业务解读 | 单一归因结论 | **两段式：数据内可证 + 明确边界** |
| 质量校验 | 文档中的规则表 | **可执行断言脚本（8 项）** |
| 阶段三定位 | Hive/Spark 重写一遍 | **引擎迁移 + 倾斜处理 + 性能基准** |
| 简历段落 | 无数字、亮点隐形 | **量化 + 亮点前置 + GitHub 链接** |
| 面试层次 | 3 层 | **4 层（新增"我的判断与边界"）** |
| 量化清单 | 无 | **12 项指标实测表** |
