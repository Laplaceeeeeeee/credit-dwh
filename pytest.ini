[pytest]
markers =
    integration: 需要数据库/集群的测试（CI 中跳过）
testpaths = tests
addopts = -q
# ⚠️ 必须把仓库根加进 sys.path：CI 里用的是裸 `pytest tests/`（不是 `python -m pytest`），
#    此时 pytest 只会把 tests/ 自己插进 sys.path，`from src.metrics import ...` 会
#    ModuleNotFoundError。这一行是让"本地命令"与"CI 命令"完全一致的关键。
pythonpath = .
